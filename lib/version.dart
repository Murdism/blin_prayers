/// Human-readable release identifiers shown inside the offline app.
/// Keep [appVersion] and [buildNumber] aligned with `pubspec.yaml`.
const appVersion = '2.0.0';
const buildNumber = 2;
const contentVersion = '2026.09.05';
